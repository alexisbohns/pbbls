# @pbbls/shared

Shared types, constants, and utilities used across apps and packages. Currently a **stub**.

## Status

Placeholder package. Build and lint scripts are no-ops. Entry point `src/index.ts` is an empty export.

## When development begins

- Domain types will migrate here from `apps/web/lib/types.ts`.
- Shared constants (emotions, domains, card types) may migrate from `apps/web/lib/config/`.
- Validation schemas and pure utility functions shared across `apps/web/` and `apps/ios/`.
