# ProGuard/R8 rules. The debug build (the only artifact this milestone ships)
# is not minified, so these rules are inert for now; they exist so the release
# buildType's proguardFiles reference resolves. Real keep-rules land when a
# release/minified build is introduced in a future milestone.
